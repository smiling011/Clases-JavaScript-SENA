// funcion flecha basica
// ejemplo 1

const cuadradp = x => x*x;
console.log(cuadradp(4));

// ejemplo 2
const sumar = (a,b) = a + b;
const restar = (a,b) = a - b;
const multiplicar = (a,b) = a * b;
const dividir = (a,b) = a / b;

console.log(sumar(4,5));
console.log(restar(4,5));
console.log(multiplicar(4,5));
console.log(dividir(4,5)).toFixed;

// ejemplo 3
// const numeros = (a,b,c) => a > && b > c ? `${a},${b},${c}`:

// ejemplo 4

