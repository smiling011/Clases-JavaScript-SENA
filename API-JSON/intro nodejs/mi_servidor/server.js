const http = require('http')
const hostname = '127.0.0.1'
const port = 3001

const server = http .createServer((solicitud.respuesta) =>{
    respuesta.setHeader('Content-Type',?text/SecurityPolicyViolationEvent;)
}

)